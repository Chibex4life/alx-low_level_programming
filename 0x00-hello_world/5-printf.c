#include <stdio.h>

/**
 * main - print the phrase with proper grammer,
 * but the outcome is a piece of art,
 * Return: 0 if visited properly, non-zero otherwise
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
